<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.bus.ticket.dao.BusLineDao">

    <resultMap type="com.bus.ticket.entity.BusLine" id="BusLineMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="departure" column="departure" jdbcType="VARCHAR"/>
        <result property="destination" column="destination" jdbcType="VARCHAR"/>
        <result property="departureStation" column="departure_station" jdbcType="VARCHAR"/>
        <result property="arrivalStation" column="arrival_station" jdbcType="VARCHAR"/>
        <result property="stationFare" column="station_fare" jdbcType="INTEGER"/>
        <result property="platformFare" column="platform_fare" jdbcType="INTEGER"/>
        <result property="platformRevenue" column="platform_revenue" jdbcType="INTEGER"/>
        <result property="creationTime" column="creation_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="BusLineMap">
        select id,
               departure,
               destination,
               departure_station,
               arrival_station,
               station_fare,
               platform_fare,
               platform_revenue,
               creation_time,
               update_time
        from bus_line
        where id = #{id}
    </select>

    <select id="queryAll" resultMap="BusLineMap" parameterType="com.bus.ticket.model.query.BusLinePageQuery">
        select
        id, departure, destination, departure_station, arrival_station, station_fare, platform_fare, platform_revenue,
        creation_time, update_time
        from bus_line
        <include refid="where_query"/>
        <if test="orderBy != null and orderType != null">
            order by ${orderBy} ${orderType}
        </if>
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="BusLineMap" parameterType="com.bus.ticket.model.query.BusLinePageQuery">
        select
        id, departure, destination, departure_station, arrival_station, station_fare, platform_fare, platform_revenue,
        creation_time, update_time
        from bus_line
        <include refid="where_query"/>
        <if test="orderBy != null and orderType != null">
            order by ${orderBy} ${orderType}
        </if>
        limit #{startIndex}, #{endIndex}
    </select>

    <sql id="where_query">
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="departure != null and departure != ''">
                and departure = #{departure}
            </if>
            <if test="destination != null and destination != ''">
                and destination = #{destination}
            </if>
            <if test="departureStation != null and departureStation != ''">
                and departure_station = #{departureStation}
            </if>
            <if test="arrivalStation != null and arrivalStation != ''">
                and arrival_station = #{arrivalStation}
            </if>
            <if test="creationTime != null">
                and creation_time = #{creationTime}
            </if>
            <if test="updateTime != null">
                and update_time = #{updateTime}
            </if>
        </where>
    </sql>

    <!--统计总行数-->
    <select id="count" resultType="java.lang.Integer" parameterType="com.bus.ticket.model.query.BusLinePageQuery">
        select count(1) from bus_line
        <include refid="where_query"/>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bus_line(departure, destination, departure_station, arrival_station, station_fare, platform_fare,
                             platform_revenue, creation_time, update_time)
        values (#{departure}, #{destination}, #{departureStation}, #{arrivalStation}, #{stationFare}, #{platformFare},
                #{platformRevenue}, #{creationTime}, #{updateTime})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bus_line(departure, destination, departure_station, arrival_station, station_fare, platform_fare,
        platform_revenue, creation_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.departure}, #{entity.destination}, #{entity.departureStation}, #{entity.arrivalStation},
            #{entity.stationFare}, #{entity.platformFare}, #{entity.platformRevenue}, #{entity.creationTime},
            #{entity.updateTime})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bus_line(departure, destination, departure_station, arrival_station, station_fare, platform_fare,
        platform_revenue, creation_time, update_time)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.departure}, #{entity.destination}, #{entity.departureStation}, #{entity.arrivalStation},
            #{entity.stationFare}, #{entity.platformFare}, #{entity.platformRevenue}, #{entity.creationTime},
            #{entity.updateTime})
        </foreach>
        on duplicate key update
        departure = values(departure),
        destination = values(destination),
        departure_station = values(departure_station),
        arrival_station = values(arrival_station),
        station_fare = values(station_fare),
        platform_fare = values(platform_fare),
        platform_revenue = values(platform_revenue),
        creation_time = values(creation_time),
        update_time = values(update_time)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bus_line
        <set>
            <if test="departure != null and departure != ''">
                departure = #{departure},
            </if>
            <if test="destination != null and destination != ''">
                destination = #{destination},
            </if>
            <if test="departureStation != null and departureStation != ''">
                departure_station = #{departureStation},
            </if>
            <if test="arrivalStation != null and arrivalStation != ''">
                arrival_station = #{arrivalStation},
            </if>
            <if test="stationFare != null">
                station_fare = #{stationFare},
            </if>
            <if test="platformFare != null">
                platform_fare = #{platformFare},
            </if>
            <if test="platformRevenue != null">
                platform_revenue = #{platformRevenue},
            </if>
            <if test="creationTime != null">
                creation_time = #{creationTime},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from bus_line
        where id = #{id}
    </delete>

</mapper>

